{
    "scripts": {
        "prepare": "husky",
        "test": "jest --no-cache",
        "test-cov": "jest --no-cache --coverage"
    },
    "workspaces": [
        "memory-viz",
        "demo",
        "docs"
    ],
    "devDependencies": {
        "husky": "^9.1.5",
        "lint-staged": "^15.2.10",
        "prettier": "2.8.1"
    },
    "lint-staged": {
        "**/*": "prettier --write --ignore-unknown"
    }
}
