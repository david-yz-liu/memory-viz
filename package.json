{
    "scripts": {
        "prepare": "husky install",
        "test": "jest --no-cache",
        "test-cov": "jest --no-cache --coverage"
    },
    "workspaces": [
        "memory-viz",
        "demo",
        "docs"
    ],
    "devDependencies": {
        "child_process": "^1.0.2",
        "husky": "^8.0.3",
        "lint-staged": "^14.0.1",
        "prettier": "2.8.1"
    },
    "lint-staged": {
        "**/*": "prettier --write --ignore-unknown"
    }
}
