{
    "scripts": {
        "prepare": "husky install",
        "test": "jest --no-cache",
        "test-cov": "jest --no-cache --coverage"
    },
    "workspaces": [
        "memory-viz",
        "demo",
        "docs"
    ],
    "devDependencies": {
        "husky": "^8.0.3",
        "lint-staged": "^14.0.1",
        "prettier": "2.8.1",
        "tmp": "^0.2.3"
    },
    "lint-staged": {
        "**/*": "prettier --write --ignore-unknown"
    }
}
