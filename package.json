{
    "scripts": {
        "prepare": "husky",
        "test": "jest --no-cache",
        "test-cov": "jest --no-cache --coverage"
    },
    "workspaces": [
        "memory-viz",
        "demo",
        "docs",
        "webstepper"
    ],
    "devDependencies": {
        "husky": "^9.1.5",
        "lint-staged": "^15.2.10",
        "prettier": "3.4.1"
    },
    "lint-staged": {
        "**/*": "prettier --write --ignore-unknown"
    },
    "dependencies": {
        "@types/pretty": "^2.0.3"
    }
}
